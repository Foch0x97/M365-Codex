# 复制为 .env.local 后按需修改（.env* 已在仓库根 .gitignore 里排除，只有 .env.example 会入库）。

# 设为 1 时整站切到 src/api/mock.ts 的内存模拟数据，脱离服务端独立开发/联调 UI。
VITE_USE_MOCK=0

# 开发代理目标：vite.config.ts 会把 /admin、/v1 转发到这里，默认已经是服务端的默认端口。
VITE_API_TARGET=http://127.0.0.1:8080
