/** 尚未完善的页面占位：能正常进入、不白屏，避免用户点导航时以为应用挂了。 */
export declare function PlaceholderPage({ title, subtitle, note }: {
    title: string;
    subtitle: string;
    note: string;
}): import("react").JSX.Element;
