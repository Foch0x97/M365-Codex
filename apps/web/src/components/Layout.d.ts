import type { ReactNode } from 'react';
export declare function Layout({ children, title, subtitle }: {
    children: ReactNode;
    title: string;
    subtitle?: string;
}): import("react").JSX.Element;
